<a href="/user/<%= @user.id %>/edit">Edit Blogger Content</a>

<h1>Welcome, Blogger <%= @user.first_name %></h1>

<% if flash[:enter_success] %>
	<div class="alert alert-success alert-dismissible" role="alert">
  		<button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
  		<%= flash[:enter_success] %>
	</div>
<% end %>

<p>It has been XX days since you started blogging. You have completed XX tallies. </p>
<p>There are still XX tallies to be explored.</p>

<div class="col-md-12">
<form class="form-horizontal col-md-12" method="post" action="/status">
	<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
  	<div class="form-group">
    	<label class="col-xs-2 control-label">Status Type</label>
	    <div class="col-xs-10">
	      <input type="text" class="form-control" name="status[type]" placeholder="Enter a Status Type">
	    </div>
  	</div>
  	<input type="hidden" class="form-control" name="user[id]" value="<%= @user.id %>">
  	<button type="submit" class="btn btn-default">Submit</button>
</form>
</div>